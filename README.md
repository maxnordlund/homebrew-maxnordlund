# Maxnordlund Maxnordlund

## How do I install these formulae?

`brew install maxnordlund/maxnordlund/<formula>`

Or `brew tap maxnordlund/maxnordlund` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
